task E {
  int steps = 0;

  input perform(int n) {
    if (n % 2 == 0) {
      n = n/2;
      steps++;
    }
    if (n != 1) {
      send perform(n) to O;
    } else {
      send result(steps) to O;
    }
  }

  input result(int s) {
    steps = steps + s;
    send result(steps) to I;
  }

}
